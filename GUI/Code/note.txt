NOTIFICHE:
 - formattare data e ora nella tabella
 - gestire status


// response endpoint /alert GET:
[
    [{
        id: number
        timestamp: string
        severity: number
        machineID: number
        KPI: string
        alertDescription: string
        explanation: string
        solved: boolean
    }]
]


KPI:
// response endpoint /KPI GET:
[
    {
        "id": number,
        "name": string,
        "category": "TIME_KPI" | "ENERGY_COST_KPI" | "PRODUCTION_KPI",
        "isBase": boolean,
        "measureUnit": string      //(es. h, kw\h, kg ...)
    }
]
// response endpoint ..... GET (lista degli id e nome delle macchine associate ad uno specifico KPI):
[
    {
        "id": number,
        "name": string
    },
]
// response endpoint ..... GET (valori di uno specifico KPI su una specifica macchina passando anche l'arco temporale ('day', 'week', 'month')):
[
    {
        "machineID": number,
        "nameKPI": string,
        "unit": string,
        "code": 0,
        "timeScale": 'hour', 'day', 'week', 'month',
        "series": [
            {
                "time": string,    // timestamp
                "value": number
            }
        ]
        "averageDay": number,
        "averageWeek": number,
        "averageMonth": number
    }
]


RAG:
// response endpoint /userquery POST
{
    "type": “textual” | “dashboard” | "report",
    “text”: string (the textual response generated by the LLM) | None,
    “dashboard”: { 
 "x_axis_name": x_axis_name,  # Name of the X-axis
        "y_axis_name": y_axis_name,  # Name of the Y-axis
        "values": values,  # Data points for the dashboard (e.g., {"x": "date", "y": value} or {"start_date":  "date", "end_date": "date", "value": value})
    } | None,
    "report": {
 "title": string,
        "sections": {
            "Key Performance Metrics": [list of strings],
            "Overall Trends": [list of strings],
            "Observations": [list of strings]
    } | None
}